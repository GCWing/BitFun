import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const iconCatalogAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'icon-catalog',
  parts: [{ id: 'root' }, { id: 'item' }],
};
