import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';
export const capabilityBarAppearanceDescriptor: AppearanceSurfaceDescriptor = { id: 'capability-bar', parts: [{ id: 'root' }, { id: 'label' }, { id: 'items' }, { id: 'warn' }] };
