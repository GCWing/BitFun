import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const workflowClawGalleryAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'workflow-claw-gallery',
  parts: [{ id: 'root' }],
};
