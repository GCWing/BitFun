import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance';

export const createGroupChatDialogAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'create-group-chat-dialog',
  parts: [
    { id: 'root' },
  ],
};
