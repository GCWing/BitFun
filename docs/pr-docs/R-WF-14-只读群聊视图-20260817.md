# R-WF-14 前端只读视图 · PR 说明（worktree 落盘副本）

> 与知识库 `03-执行/R-WF-14-执行记录-20260817.md` 同源（本文件 = pr-docs 镜像，供合入 main 后归档）
> 提交：`feat(ui): read-only group log view` = ee4636cf1（task/rwf14）

## 改动摘要

群聊会话打开 = **只读气泡时间线**（复用 FlowChat 气泡管线，无输入框/无成员表/无交互），
普通会话仍走 ChatPane（路由不误伤），isGroupChat 从后端 metadata（customMetadata.groupChats）恢复。

| 文件 | 变更 |
|---|---|
| `src/web-ui/src/app/scenes/session/GroupLogView.tsx`（新） | 只读视图：get_group_history → flowChatStore.addDialogTurn → ModernFlowChatContainer |
| `src/web-ui/src/app/scenes/session/groupMessageProjection.ts`（新） | GroupChatView 与 GroupLogView 共享的消息→DialogTurn 投影（单一源） |
| `src/web-ui/src/app/scenes/session/SessionScene.tsx` | 群聊分支路由到 GroupLogView；清理未用 useWorkspaceContext |
| `src/web-ui/src/flow_chat/store/FlowChatStore.ts` | isGroupChat metadata 恢复注释更新 + W1f 串行独占标注（P2-1） |
| `src/web-ui/src/app/scenes/session/GroupLogView.scss/.appearance.ts`（新） | 样式 + 外观契约（无 input/toolbar part） |
| `src/web-ui/src/infrastructure/appearance/registry/defaultAppearanceRegistry.ts` | 注册 groupLogView surface |
| 测试 ×3（新） | RO-1 只读 / RO-2 气泡+重试 / RO-3 路由（群聊 vs 普通） |

## 验证

- tsc 0 error / i18n:audit 0 / i18n:contract 37 pass / appearance audit passed / eslint 0w0e
- vitest：session 域 23 tests + FlowChatStore 131 tests 全绿
